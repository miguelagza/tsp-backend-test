I didn't really had an approach for the TSP as I haven't solved or seen that problem before, also I don't have too much experience in solving graphs or LeetCode like problems plus I still don't know how NestJS works at all because I have only programmed in plain Node.js, but I looked some NestJS documentation and searched a lot of videos and articles talking about the possible or optimal solutions in this case and explored those algorithms given the time and space complexity. So I picked the one that could solve this problem for number of cities > 10 but less than 20 or so, and found that the tecnique called "Dynamic Programming" could solve this situation with O(n^2 * 2n) instead of O(n!) if I used backtracking. An important trade-off was that I only calculated the minimum distance of the path as I couldn't implement the exact path from city to city.
